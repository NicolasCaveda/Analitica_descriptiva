requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0

# Descompresion HTTP. Sin estos, si el servidor responde comprimido con
# Brotli/zstd el cuerpo llega sin decodificar: status 200 y CERO avisos.
# Ese fue el bug que dejaba a Remax en 0 (ver NOTA en src/utils.py).
Brotli>=1.1.0
zstandard>=0.22.0

# Imita la huella TLS de Chrome. No es indispensable para Remax (no tiene
# fingerprinting agresivo), pero no hace dano tenerlo.
curl_cffi>=0.7.0

# Solo para los tests de test_parseo.py (first_text/first_elements de
# src/base.py, pensados para portales HTML). Remax es API JSON pura y no
# la necesita para scrapear.
beautifulsoup4>=4.12.0

# Graficos de notebooks/02_graficos.ipynb
matplotlib>=3.7.0
